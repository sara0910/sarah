const a = 10;
const b = 20;

function sum(a1, b1) {
  return a1 + b1;
}

sum(a, b);

function multiply(n, m) {
  return n * m;
}

multiply(a, b);

function division(c1, d1) {
  return c1 / d1;
}
division(a, b);
